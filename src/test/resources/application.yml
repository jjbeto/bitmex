# test URL: https://testnet.bitmex.com/api/v1
# prod URL: https://www.bitmex.com/api/v1
app:
  url: ${BITMEX_URL}
  key: ${BITMEX_KEY}
  secret: ${BITMEX_PASSWORD}

spring:
  main:
    allow-bean-definition-overriding: true
